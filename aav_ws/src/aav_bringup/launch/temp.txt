PKG=~/ros2_ws/src/ardupilot_msgs; SRC=~/ros2_ws/src; WS=~/ros2_ws; if [ ! -d "$PKG" ]; then echo "Fetching ardupilot_msgs via sparse-checkout..."; tmp=/tmp/ardupilot_sparse; rm -rf "$tmp"; git clone --filter=blob:none --no-checkout https://github.com/ArduPilot/ardupilot.git "$tmp" && cd "$tmp" && git sparse-checkout init --cone && git sparse-checkout set Tools/ros2/ardupilot_msgs && git checkout master && mkdir -p "$SRC" && cp -a Tools/ros2/ardupilot_msgs "$PKG" && cd "$WS" && rm -rf "$tmp"; else echo "ardupilot_msgs already present, skipping fetch"; fi