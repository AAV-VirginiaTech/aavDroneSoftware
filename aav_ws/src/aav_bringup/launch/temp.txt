# ===== HARD RESET TORCH ENV =====
python3 -m pip uninstall -y torch torchvision torchaudio

# Remove ALL leftover torch files (this is what’s been breaking you)
rm -rf ~/.local/lib/python3.10/site-packages/torch*
rm -rf ~/.local/lib/python3.10/site-packages/torchvision*
rm -rf ~/.local/lib/python3.10/site-packages/torchaudio*

# Also remove any system leftovers just in case
sudo rm -rf /usr/local/lib/python3.10/dist-packages/torch*
sudo rm -rf /usr/local/lib/python3.10/dist-packages/torchvision*
sudo rm -rf /usr/local/lib/python3.10/dist-packages/torchaudio*

# ===== REINSTALL CLEAN JETSON TORCH =====
cd ~/aavDroneSoftware   # adjust if wheel is elsewhere

pip3 install --no-cache-dir ./torch-2.4.0a0+07cecf4168.nv24.05*.whl

# ===== TEST TORCH ONLY (STOP IF THIS FAILS) =====
python3 - << 'PY'
import torch
print("torch:", torch.__version__)
print("cuda:", torch.cuda.is_available())
print("gpu:", torch.cuda.get_device_name(0) if torch.cuda.is_available() else "NONE")
PY

# ===== ONLY AFTER TORCH WORKS =====
pip3 install torchvision==0.19.0 --no-deps

# ===== FINAL TEST =====
python3 - << 'PY'
import torch, torchvision
print("torch:", torch.__version__)
print("torchvision:", torchvision.__version__)
print("cuda:", torch.cuda.is_available())
PY