pip uninstall -y torch torchvision torchaudio
pip cache purge


sudo apt-get update
sudo apt-get install -y libopenblas-dev libopenmpi-dev libomp-dev libjpeg-dev zlib1g-dev libpython3-dev libavcodec-dev libavformat-dev libswscale-dev




# This points to the index for JetPack 6 (jp6) and CUDA 12.2 (cu122)
pip install --no-cache-dir --extra-index-url https://pypi.jetson-ai-lab.dev/jp6/cu122 torch torchvision



python3 -c "import torch; import torchvision; print(f'CUDA available: {torch.cuda.is_available()}'); print(f'Device Name: {torch.cuda.get_device_name(0) if torch.cuda.is_available() else \"None\"}'); print(f'Torchvision Version: {torchvision.__version__}')"