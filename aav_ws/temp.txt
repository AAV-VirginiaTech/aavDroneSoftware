# Install build deps for torchvision
sudo apt install -y libjpeg-dev zlib1g-dev libpng-dev

# Build/install torchvision 0.18 against your working Jetson torch 2.3
export BUILD_VERSION=0.18.0
python3 -m pip install --no-cache-dir --no-deps --no-build-isolation \
  "git+https://github.com/pytorch/vision.git@v0.18.0"

# Test torch + torchvision + ultralytics
python3 -c "import torch; print('torch:', torch.__version__, torch.cuda.is_available())"
python3 -c "import torchvision; print('torchvision:', torchvision.__version__)"
python3 -c "from ultralytics import YOLO; print('ultralytics ok')"